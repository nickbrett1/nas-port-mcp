# nas-port-mcp
# Host networking: the container binds directly on the host (no ports: mapping).

# Port the mcpo Streamable HTTP proxy listens on (loopback; front with `tailscale serve`).
MCP_PORT=3001
# MCP_TRANSPORT=streamable-http
